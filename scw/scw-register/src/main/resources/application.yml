spring:
  application:
    name: scw-register
server:
  port: 8761

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false #注册中心不需要在注册自己
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
